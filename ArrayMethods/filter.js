const words = ['spray', 'limit', 'elite', 'exuberant']

const result = words.filter(word => word.length > 6)

console.log(result)

// Array.filter(function(currentValue, index, arr), thisValue => condition)
// index, arr, thisValue - optional