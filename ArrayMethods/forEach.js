let arr = [1,2,3]

arr.forEach((value, index) => {

    arr[index] = 10

})

// using index we can modify our array
// using item we can mldify the array only when the item is an object

console.log(arr)